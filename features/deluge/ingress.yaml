apiVersion: v1
kind: Service
metadata:
  name: deluge-web
spec:
  selector:
    app: deluge
  ports:
  - name: http
    port: 80
    targetPort: http
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: deluge-ingress
  labels:
    ingress: int
spec:
  entryPoints:
  - websecure
  routes:
  - match: Host(`deluge.int.noodoodle.us`)
    services:
    - name: deluge-web
      namespace: media-stack
      port: 80
