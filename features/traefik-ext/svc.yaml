apiVersion: v1
kind: Service
metadata:
  name: traefik-ext
spec:
  ports:
  - name: web-ext
    port: 80
    targetPort: web-ext
  - name: websecure-ext
    port: 443
    targetPort: websecure-ext
  selector:
    app.kubernetes.io/name: traefik
  type: ClusterIP
