apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
patches:
- target:
    kind: IngressRoute
    labelSelector: ingress=int
  patch: |
    - op: replace
      path: /metadata/namespace
      value: ingress
    - op: add
      path: /spec/tls
      value:
        secretName: wildcard-noodoodle-cert
