apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
patches:
- target:
    kind: Service
    name: traefik-ext
    namespace: ingress
  patch: |
    - op: replace
      path: /spec/type
      value: LoadBalancer
    - op: add
      path: /spec/loadBalancerClass
      value: tailscale
